Startup is blocked by context.dependencyBlockers. Each blocker names the repository, affected services, stable cause, requiredAction, worktreePath, and logPath when available. Repair deterministic problems within the authorized worktree and dependency configuration without asking for confirmation. Ask the user only when recovery requires a genuine choice or authority you do not have, such as changing a shared checkout outside this run. Do not invent alternatives such as continuing anyway, marking dependencies compatible, or changing the tested revision. Different generator inputs do not prove that generated output differs. Fix app/service dependencies, not tests; never weaken the validator to bypass the gate. After the repair, signal_run once with kind:"restart", hypothesis, and fixDescription, then wait_for_heal_task on the same run. Both restart and rerun recheck dependencies and persist fresh evidence before any service spawn or test verification. A still-incompatible result remains blocked; use its updated requiredAction. get_heal_context recovers this state after a missed response or reconnect.
