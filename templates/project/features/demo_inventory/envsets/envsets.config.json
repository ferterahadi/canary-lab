{
  "appRoots": {},
  "slots": {
    "demo_inventory.env": {
      "description": "Environment file for the demo storefront's inventory service",
      "target": "$CANARY_LAB_PROJECT_ROOT/features/demo_inventory/.env"
    }
  },
  "feature": {
    "slots": ["demo_inventory.env"],
    "testCommand": "npx playwright test",
    "testCwd": "$CANARY_LAB_PROJECT_ROOT/features/demo_inventory"
  }
}
