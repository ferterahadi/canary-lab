GATEWAY_URL=http://localhost:4400
# Read by the specs, not by the service — it is here so the scaffold shows an
# envset carrying real test configuration rather than only a base URL.
LOW_STOCK_THRESHOLD=5
