# The captured environment for the local envset.
#
# The three services take their port from the run itself — see the `ports` slots
# in feature.config.cjs — so nothing about addressing lives here. What is left is
# the storefront's own configuration, which is what a real suite's envset holds.
STOREFRONT_CURRENCY=SGD
STOREFRONT_WELCOME_CODE=WELCOME10
