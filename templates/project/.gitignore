node_modules/
logs/
playwright-report/
test-results/
features/*/.env
*.log
*.bak*
.DS_Store

# Ignore files inside envset variant dirs (may contain secrets).
# envsets.config.json is one level up, so it stays tracked.
features/*/envsets/*/*
!features/example_todo_api/envsets/local/
!features/broken_todo_api/envsets/local/
