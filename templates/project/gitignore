node_modules/
logs/
playwright-report/
test-results/
features/*/.env
*.log
*.bak*
.DS_Store

# Ignore files inside envset variant dirs (may contain secrets).
# envsets.config.json is one level up, so it stays tracked.
envsets/*/*
features/*/envsets/*/*
!features/demo_catalog/envsets/local/*
!features/demo_inventory/envsets/local/*
